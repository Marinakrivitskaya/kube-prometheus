apiVersion: v1
kind: Service
metadata:
  labels:
    app: my-diplom
  name: my-diplom
  namespace: monitoring
spec:
  type: NodePort
  ports:
  - name: http
     port: 80
     targetPort: http
    nodePort: 8090
  #externalIPs:
  #  - 192.168.49.1
  selector:
    app: my-diplom
